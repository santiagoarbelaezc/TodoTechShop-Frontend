version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build --configuration=production
  artifacts:
    baseDirectory: dist/todo-tech-front/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*